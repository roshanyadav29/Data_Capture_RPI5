{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build & Run",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/gpio_sampler",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "build"
        }
    ]
}